# Portfolio Home Page

## Overview

This is a simple static portfolio home page built with HTML, CSS, and JavaScript. It is designed as a clean starting point for a personal portfolio website.

## Features

- Minimal home page layout
- Responsive card-style design
- Separate CSS and JavaScript files
- Easy to extend with more sections later
- Simple folder structure for better project organization

## Collaborators

- Taha Sohail

## Technologies Used

- HTML5
- CSS3
- JavaScript

## How to Run

This project does not require any installation or build step.

### Option 1: Open the file directly
1. Open `index.html` in your browser.
2. The page will load immediately.

### Option 2: Use VS Code Live Server
1. Open the project folder in VS Code.
2. Install the Live Server extension if needed.
3. Right-click `index.html` and select **Open with Live Server**.

## Setup Notes

- Make sure the `assets/css/style.css` and `assets/js/main.js` files stay linked in `index.html`.
- Use the `assets/images` folder for any future images or profile pictures.
- If you add new pages, keep the file structure organized inside the project folder.
- Commit changes regularly if you are pushing the project to GitHub.

## Project Structure

```text
Internship_CODOC/
├── .gitignore
├── README.md
├── index.html
└── assets/
    ├── css/
    │   └── style.css
    ├── images/
    └── js/
        └── main.js
```

## Notes

- The `assets/images` folder is ready for future images.
- You can expand this project by adding sections like About, Projects, and Contact.
- If you want to publish it online, you can deploy it to GitHub Pages or any static hosting service.
